// Write your code in this file!

let currentUser = "Slim Jim Phantom";
// currentUser is defined

let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;
// welcomeMessage contains "Welcome to Flatbook, " + contains the value of the 'currentUser' variable + ends with an exclamation point!

let excitedWelcomeMessage = welcomeMessage.toUpperCase();
// excitedWelcomeMessage contains "WELCOME TO FLATBOOK, " + contains the value of the 'currentUser' variable + ends with an exclamation point

let shortGreeting = "Welcome, " + currentUser[0]+"!"
//shortGreeting contains "Welcome, " contains the first initial of the name stored in the 'currentUser' variable + ends with an exclamation point